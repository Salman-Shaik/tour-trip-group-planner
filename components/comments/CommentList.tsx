import { MessageCircle } from "lucide-react";
import { DeleteCommentButton } from "@/components/comments/DeleteCommentButton";
import type { Comment,Participant } from "@/lib/types";

type DisplayComment=Comment&{participant:Participant};
export function CommentList({comments,currentParticipantId,inviteCode,listingId}:{comments:DisplayComment[];currentParticipantId:string|null;inviteCode:string;listingId:string}){if(!comments.length)return <div className="rounded-2xl border border-dashed border-[#cfd8d4] px-5 py-10 text-center"><MessageCircle className="mx-auto text-[#80938d]" size={30}/><p className="mt-3 font-bold">No comments yet</p><p className="mt-1 text-sm text-[#71807c]">Start the conversation about this stay.</p></div>;return <div className="space-y-3">{comments.map((comment)=><article key={comment.id} className="rounded-2xl border border-[#e3e0d8] bg-white p-4"><div className="flex items-start gap-3"><span className="grid size-9 shrink-0 place-items-center rounded-full bg-[#e7f1ed] text-sm font-extrabold text-[#285f54]">{comment.participant.name.slice(0,1).toUpperCase()}</span><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><div><p className="text-sm font-bold">{comment.participant.name}{comment.participantId===currentParticipantId&&<span className="ml-1.5 text-xs text-[#1f7168]">You</span>}</p><time className="text-xs text-[#87948f]" dateTime={comment.createdAt}>{new Intl.DateTimeFormat("en",{dateStyle:"medium",timeStyle:"short"}).format(new Date(comment.createdAt))}</time></div>{comment.participantId===currentParticipantId&&<DeleteCommentButton inviteCode={inviteCode} listingId={listingId} commentId={comment.id}/>}</div><p className="mt-3 whitespace-pre-wrap break-words leading-7 text-[#425651]">{comment.body}</p></div></div></article>)}</div>}
