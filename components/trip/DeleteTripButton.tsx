"use client";
import { useState,useTransition } from "react";
import { LoaderCircle,Trash2,X } from "lucide-react";
import { deleteTrip } from "@/actions/settings";
export function DeleteTripButton({inviteCode,tripName}:{inviteCode:string;tripName:string}){const[open,setOpen]=useState(false);const[confirmation,setConfirmation]=useState("");const[pending,startTransition]=useTransition();const matches=confirmation===tripName;if(!open)return <button type="button" onClick={()=>setOpen(true)} className="inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-full border border-[#d85c48] px-5 text-sm font-bold text-[#b33d2a]"><Trash2 size={17}/>Delete trip</button>;return <div className="rounded-2xl border border-[#efb5aa] bg-[#fff5f2] p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-extrabold text-[#8f2f20]">Permanently delete this trip?</h3><p className="mt-1 text-sm leading-6 text-[#76534d]">Stays, participants, votes, comments, and preference choices will also be removed. This cannot be undone.</p></div><button type="button" aria-label="Cancel deletion" onClick={()=>{setOpen(false);setConfirmation("")}} className="grid size-9 shrink-0 place-items-center rounded-full"><X size={17}/></button></div><label htmlFor="delete-confirmation" className="mt-4 block text-sm font-bold">Type <span className="select-all">{tripName}</span> to confirm</label><input id="delete-confirmation" value={confirmation} onChange={(event)=>setConfirmation(event.target.value)} autoComplete="off" className="mt-2 min-h-11 w-full rounded-xl border border-[#d9aaa0] bg-white px-3 outline-none focus:border-[#b33d2a]"/><button type="button" disabled={!matches||pending} onClick={()=>startTransition(()=>deleteTrip(inviteCode))} className="mt-3 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-full bg-[#b33d2a] px-5 text-sm font-bold text-white disabled:cursor-not-allowed disabled:opacity-45">{pending?<LoaderCircle className="animate-spin" size={17}/>:<Trash2 size={17}/>}Delete permanently</button></div>}
