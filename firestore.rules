rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Roamly accesses Firestore only through its authenticated server runtime.
    // No browser/client SDK is permitted to read trip or account data directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
