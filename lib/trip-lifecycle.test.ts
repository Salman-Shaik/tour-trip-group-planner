import { describe,expect,it } from "vitest";
import { getTripLifecycle,groupTrips } from "@/lib/trip-lifecycle";
import type { Trip } from "@/lib/types";
const trip=(overrides:Partial<Trip>={}):Trip=>({id:"1",inviteCode:"test",name:"Test",destination:"Goa",startDate:"2026-08-01",endDate:"2026-08-10",travellerCount:2,accommodationBudget:null,currency:"INR",creatorUserId:"user",creatorTokenHash:"",status:"ACTIVE",completedAt:null,selectedListingId:null,createdAt:"",updatedAt:"",...overrides});
describe("trip lifecycle",()=>{it("keeps future trips active",()=>expect(getTripLifecycle(trip({endDate:"2026-09-01"}),"2026-08-25")).toBe("ACTIVE"));it("derives past from the end date",()=>expect(getTripLifecycle(trip(),"2026-08-25")).toBe("PAST"));it("manual completion takes priority",()=>expect(getTripLifecycle(trip({status:"COMPLETED",endDate:"2027-01-01"}),"2026-08-25")).toBe("COMPLETED"));it("groups multiple trips without losing them",()=>{const groups=groupTrips([trip(),trip({id:"2",endDate:"2027-01-01"}),trip({id:"3",status:"COMPLETED"})],"2026-08-25");expect(groups.PAST).toHaveLength(1);expect(groups.ACTIVE).toHaveLength(1);expect(groups.COMPLETED).toHaveLength(1);});});
