import { expect,test } from "@playwright/test";
test("home is responsive and offers creator sign in",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Find the stay/})).toBeVisible();await expect(page.getByRole("button",{name:"Sign in"})).toBeVisible();await expect(page.getByRole("link",{name:"Plan a trip"})).toBeVisible();});
test("new trip explains account ownership",async({page})=>{await page.goto("/trips/new");await expect(page.getByRole("heading",{name:"Sign in to create a trip"})).toBeVisible();await expect(page.getByRole("button",{name:"Continue with Google"})).toBeVisible();});
test("development demo still supports creator settings",async({page})=>{await page.goto("/");await page.getByRole("button",{name:/Open as creator/}).click();await expect(page.getByRole("heading",{name:"Goa December Trip"})).toBeVisible();await page.locator('a[href$="/settings"]:visible').first().click();await expect(page.getByRole("heading",{name:"Trip settings"})).toBeVisible();await expect(page.getByRole("button",{name:"Mark as completed"})).toBeVisible();await expect(page.getByRole("button",{name:"Delete trip"})).toBeVisible();});
